name: Build DawaCheck APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.0'
          channel: 'stable'

      - name: Generate Android platform folder
        run: |
          if [ ! -d "android" ]; then
            flutter create --platforms=android --org com.dawacheck --project-name dawa_check .
          fi

      - name: Create assets folder if missing
        run: |
          mkdir -p assets

      - name: Fix build.gradle (compileSdk 34, minSdk 21, Kotlin 1.9.24)
        run: |
          # android/app/build.gradle: compileSdk 34 (stable), minSdk 21
          sed -i 's/compileSdk = 35/compileSdk = 34/g' android/app/build.gradle
          sed -i 's/compileSdkVersion 35/compileSdkVersion 34/g' android/app/build.gradle
          sed -i 's/minSdkVersion flutter.minSdkVersion/minSdkVersion 21/g' android/app/build.gradle
          sed -i 's/minSdk = flutter.minSdkVersion/minSdk = 21/g' android/app/build.gradle

          # android/build.gradle: Kotlin version set to 1.9.24
          sed -i 's/ext.kotlin_version = .*/ext.kotlin_version = "1.9.24"/g' android/build.gradle
          grep -q "ext.kotlin_version" android/build.gradle || sed -i '/buildscript {/a\    ext.kotlin_version = "1.9.24"' android/build.gradle

          # Also update Kotlin plugin version in build.gradle dependencies
          sed -i 's/classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:.*"/classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"/g' android/build.gradle

      - name: Add required Android permissions
        run: |
          MANIFEST="android/app/src/main/AndroidManifest.xml"
          for PERM in \
            'android.permission.CAMERA' \
            'android.permission.INTERNET' \
            'android.permission.POST_NOTIFICATIONS' \
            'android.permission.SCHEDULE_EXACT_ALARM' \
            'android.permission.RECEIVE_BOOT_COMPLETED'
          do
            if ! grep -q "$PERM" "$MANIFEST"; then
              sed -i "0,/<application/s##<uses-permission android:name=\"$PERM\" \/>\n    <application#" "$MANIFEST"
            fi
          done

      - name: Install dependencies
        run: flutter pub get

      - name: Build APK
        run: flutter build apk --release

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: dawa-check-apk
          path: build/app/outputs/flutter-apk/app-release.apk